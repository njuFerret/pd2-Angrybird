//#include "bump.h"


//void bump::BeginContact(b2Contact *contact) {

//    GameItem *item1 = static_cast<GameItem*> (contact->GetFixtureA()->GetBody()->GetUserData());
//    GameItem *item2 = static_cast<GameItem*> (contact->GetFixtureB()->GetBody()->GetUserData());

//    id1 = item1->getId();
//    id2 = item2->getId();

//    if(id1 == 'b' && id2 == 'e'){
//        item2->collide();
//    }else if(id1 == 'e' && id2 == 'b'){
//        item1->collide();
//    }

//}
